const formHighTodo = document.body.querySelector(".form__high");
const formLowTodo = document.body.querySelector(".form__low");
const formHighTaskTodo = document.body.querySelector(".tasks__high");
const formLowTaskTodo = document.body.querySelector(".tasks__low");
const textTaskHigh = document.body.querySelector('.inputHigh');
const textTaskLow = document.body.querySelector('.inputLow');
const addClickTaskHigh = document.body.querySelector('.todo__add_high-img');
const addClickTaskLow = document.body.querySelector('.todo__add_low-img');

export {
    formHighTodo,
    formLowTodo,
    formHighTaskTodo,
    formLowTaskTodo,
    textTaskHigh,
    textTaskLow,
    addClickTaskHigh,
    addClickTaskLow
}